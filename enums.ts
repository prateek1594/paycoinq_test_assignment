export enum UserNames {
    correctUsername = 'standard_user',
    wrongUsername   = 'GUEST_USER',
}

export enum Passwords {
    correctPassword = 'secret_sauce',
    wrongPassword   = 'GUEST_PASSWORD',
}

export enum User {
    userFirstName = 'Test_first_name',
    userLastName = 'Test_last_name',
    userPostalCode = '12345',
}

export enum Messages {
    firstItem = 'Sauce Labs Backpack',
    itemCost = 'Total: $32.39',
    successMessage = 'THANK YOU FOR YOUR ORDER',
    confirmationMessage = 'our order has been dispatched, and will arrive just as fast as the pony can get there!',
}